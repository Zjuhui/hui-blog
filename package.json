{
  "name": "hui-blog",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "build:pages": "astro build && node scripts/sync-dist.mjs",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^5.15.4"
  }
}
